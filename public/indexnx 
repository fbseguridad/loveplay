<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>LovePlay 💋</title>
  <script type="module" src="./js/app.js"></script>
  <style>
    body {
      background: #000;
      color: #fff;
      font-family: Arial, sans-serif;
      text-align: center;
    }
    .media { max-width: 300px; border-radius: 10px; margin: 10px; }
    .post { background: #111; padding: 10px; border-radius: 15px; margin: 10px; }
    button, input, select {
      margin: 5px; padding: 10px; border-radius: 8px; border: none;
    }
  </style>
</head>
<body>
  <h1>🔥 LovePlay Panel 🔥</h1>

  <div id="estado">No conectado</div>

  <input id="email" placeholder="Email">
  <input id="password" type="password" placeholder="Contraseña">
  <br>
  <button onclick="registrar(email.value, password.value)">Registrar</button>
  <button onclick="login(email.value, password.value)">Login</button>
  <button onclick="logout()">Logout</button>

  <hr>

  <input id="file" type="file">
  <select id="tipo">
    <option value="foto">📸 Foto</option>
    <option value="video">🎥 Video</option>
  </select>
  <button onclick="subirArchivo(file.files[0], tipo.value)">Subir</button>

  <h2>Feed 🌀</h2>
  <div id="feed"></div>
</body>
</html>
